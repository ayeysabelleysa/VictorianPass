# VHEcoPoint Hardware Bridge Dependencies
# For Python 3.7+

# Web server for UI communication
# We use Python's built-in http.server, so no extra dependencies needed for basic use!

# Optional: If you use a physical QR scanner via USB HID or serial
# pyserial>=3.5
# pyusb>=1.2.1

# Optional: If you use a physical digital scale
# pyserial>=3.5

# Optional: If you use OpenCV for QR scanning via camera
# opencv-python>=4.8.0
# pyzbar>=0.1.9
